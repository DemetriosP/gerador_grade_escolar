<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.geradordegradeescolar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GeradorDeGradeEscolar">
        <activity
            android:name=".ui.activity.RecyclerDisciplinaActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.FormPreRequisito"
            android:exported="false" />
        <activity
            android:name=".ui.activity.FormDisciplina"
            android:exported="false" />
        <activity
            android:name=".ui.activity.FormCasdastro"
            android:exported="false" />
        <activity
            android:name=".ui.activity.FormLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>